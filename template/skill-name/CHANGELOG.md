# Changelog — skill-name

Newest first. Semver per SPEC.md: official skills arrive in this repo at
`1.0.0`; a skill that has not cleared the release gate starts at `0.1.0` =
first working draft, with `1.0.0` = first customer-facing release (passes the
release checklist). After 1.0: PATCH = fix/wording, MINOR = new backward-compatible
capability, MAJOR = breaking (renamed inputs, re-setup required). Every PR
that touches this skill adds an entry here — CI checks.

## 0.1.0 — YYYY-MM-DD

- First working draft.
